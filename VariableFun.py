# int variable
age = 50

# float
weight = 145.8

# string
name = "Trina Nixon"

# boolean
active = True

# all data types are inferred, don't have to specify what they are

# casting data type, doesn't change the type in memory just temporarily casts it as the data type
age = float(input("What is your age?"))
print(age)
print(int(age))
print(age)
